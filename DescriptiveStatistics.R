#############################################################################
# Descriptive statistics

1. Range 
range(val_vector)

2. Mean 
mean(val_vector)

3. Standard Deviation
sd(val_vector)

4. Quantile
quantile(val_vector)

5. Mode  
names(table(val_vector))[which(table(val_vector)==max(table(val_vector)))]

#############################################################################
